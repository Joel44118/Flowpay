const express = require('express');const app = express();app.use(express.json());app.post('/api/payment', (req, res) => {const { amount, cardNumber, expirationDate, cvv } = req.body;// Process payment logic here, for example:if (amount > 0 && cardNumber && expirationDate && cvv) {res.json({ success: true });} else {res.json({ success: false });}});app.listen(3000, () => {console.log('Server listening on port 3000');});